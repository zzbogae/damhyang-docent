# 협업 규칙

## 1. 브랜치와 PR

```bash
git switch -c design/room-tagging
# 작업
git add -p
git commit -m "룸 태깅 UX 초안"
git push -u origin design/room-tagging
```

`main`에 직접 푸시하지 않습니다. PR을 올리고 한 명 이상 확인 후 머지합니다.

### 브랜치 이름

| 접두어 | 용도 |
|---|---|
| `design/` | 설계 문서 · UX · 아트 |
| `app/` | 프로토타입 HTML |
| `pipe/` | 파이프라인 (파서 · 판정) |
| `doc/` | 문서 정리 · 회의록 |

---

## 2. 파일 이름 — 영문입니다

```
[프로젝트]_[종류]_[내용]_v[버전]_[YYMMDD].[확장자]
```

```
mined_doc_redesign_v1_260822.md
mined_kit_pipeline_v1.3_260822.zip
mined_deck_midterm_v1_260822.pptx
```

| 코드 | 무엇 |
|---|---|
| `doc` | 설계서 · 공유본 · 회의정리 |
| `kit` | 코드 · 실행 파일 |
| `out` | 돌려서 나온 결과물 |
| `deck` | 발표 자료 |
| `raw` | **저장소 금지** — 로컬 전용 |

### 한글 파일명을 쓰지 않는 이유 (실제로 겪은 문제)

| 어디서 | 무슨 일이 있었나 |
|---|---|
| 프로젝트 지식 | 한글 이름 문서는 읽기·**삭제 둘 다 실패**합니다 |
| 팀원 다운로드 | 브라우저·카톡에서 다운로드가 깨집니다 |
| 터미널 | 공백과 한글이 섞이면 경로가 끊깁니다 |

폴더 이름은 한글이어도 됩니다. **파일 이름만** 영문입니다.

---

## 3. 커밋 전 개인정보 확인 — 매번

```bash
git status --short
```

아래가 보이면 **멈추세요**.

- 사람 이름이 붙은 파일 (`report_bokyung.html`)
- 날짜 범위가 붙은 데이터 (`2024-03_steps.csv`)
- `.zip` · `.json` 중 내보내기 결과물
- 사진 · 스크린샷

`.gitignore`가 대부분 막지만 **`git add -f`는 그걸 무력화합니다.** 쓰지 마세요.

실수로 커밋했다면 푸시하지 말고 바로 알려주세요. 푸시된 뒤에는 이력에서 지워도 완전히 사라지지 않습니다.

---

## 4. 설계를 바꿀 때

1. **이슈를 먼저 엽니다** — 무엇을 왜 바꾸는지
2. 논의가 끝나면 `docs/`의 해당 문서를 고칩니다
3. 문서를 고치는 PR에 이슈 번호를 답니다

코드만 바뀌고 문서가 그대로면 다음 사람이 반드시 헷갈립니다. **문서가 먼저입니다.**

### 기각된 안을 다시 올리지 않습니다

봉황 / 1,400년의 거짓말 / 멈추면 사라진다 / 백제 금동대향로 / 물리 오브젝트

각각 명확한 기각 사유가 있습니다. 다시 제안하기 전에 사유를 먼저 확인하세요.

---

## 5. 표현 검토

문구를 쓰거나 고칠 때 아래를 통과해야 합니다.

- [ ] 진단어가 없다 (우울 · 번아웃 · 위험군 · 회복기)
- [ ] 좋음/나쁨 판정이 없다 (적음/많음으로 썼다)
- [ ] 근거가 함께 표시된다 (몇 건 · 어느 지표 · 어느 창)
- [ ] 확신할 수 없는 것을 확신처럼 쓰지 않았다
- [ ] 위로하려 들지 않았다
- [ ] 완전히 봉합하지 않았다 — 흔적이 남았다

민디의 말투는 `docs/`의 대사집을 기준으로 합니다. 위로하지 않고, 판정하지 않고, **"언제"까지만** 압니다. "왜"는 모른다고 매번 분명히 말합니다.
